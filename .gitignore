node_modules/
*.jks
*.keystore
*.b64
play-store-key.json
.env
.DS_Store

# Android build outputs
android/app/build/
android/.gradle/
android/local.properties

# Releases folder — AABs are tracked intentionally
!releases/
!releases/*.aab
